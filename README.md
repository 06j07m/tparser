# Tlingit verb parser

Install the working copy by:

```shell
pip install -e <path to folder>
```

Import the parser by:

```python
from tparser import Parser
```

Example usage:

```python
parser = Parser()
parser.parse_word("akaawaník")
```